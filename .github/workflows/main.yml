name: First GitHub Action
on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "hello world"
      
      - name: check out repo 
        uses: actions/checkout@v6.0.2

        
      - name: files repo 
        run: ls -la
      
